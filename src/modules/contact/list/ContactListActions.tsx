// import ContactService from "../contactService";

// const prefix = "CONTACT_LIST";
// const ContactListActions = {
//   LIST_STARTED: `${prefix}_LIST_STARTED`,
//   LIST_SUCCESS: `${prefix}_LIST_SUCCESS`,
//   LIST_ERROR: `${prefix}_LIST_ERROR`,

//   doFind: (filter,rawFilter,keepPagination= false) => async (dispatch) => {
//     try {
//       dispatch({ type: ContactListActions.LIST_STARTED });
//       dispatch({ type: ContactListActions.LIST_SUCCESS });
//       await ContactService.list(filter, rawFilter?, limit, offset);
//     } catch (error) {
//       dispatch({ type: ContactListActions.LIST_ERROR });
//     }
//   },
// };
// export default ContactListActions;

import React from "react";

function ContactListActions() {
  return <div></div>;
}

export default ContactListActions;
